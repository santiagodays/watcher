<%= form_with(model: search_bar) do |form| %>
  <% if search_bar.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(search_bar.errors.count, "error") %> prohibited this search_bar from being saved:</h2>

      <ul>
        <% search_bar.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
